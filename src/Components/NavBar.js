import React from 'react';

const NavBar = () => {
    return (
        <div>
            <button>Home</button>
            &emsp; &emsp; &emsp;
            <button>Playlist</button>
            &emsp; &emsp; &emsp;
            <button>Friends</button>
        </div>
    );
}

export default NavBar;
